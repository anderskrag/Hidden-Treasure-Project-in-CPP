#include "testing_stuff.h"


void loadBoard(std::filesystem::path filename){
    std::ifstream is{filename};
    std::string line;
    while(getline(is, line)){
        for(char tile_char : line){
            
        }
    }


}
